//use reqwest::{blocking::Client, Error};

fn main() {
    
    //in the following lines I will place the pseudo-code for the real project
    // declare a variable to get the api-key/secret from the .env file
    // define logic for putting together a get request to the api, passing the api key to it as a parameter
    
}